# 삼각형의 한 변의 길이를 side 변수로 나타낸다.
# side 변수의 초기값은 100이다.
# side 변수를 이용하여 화면에 삼각형을 그려보자.

import turtle

t = turtle.Turtle()

side = 100

t.forward(side)
t.left(120)
t.forward(side)
t.left(120)
t.forward(side)
